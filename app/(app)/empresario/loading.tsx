/**
 * Esqueleto instantáneo de el inicio del empresario. El esqueleto real vive UNA sola vez en
 * components/portales/PortalSkeletons: los dos portales comparten árbol de rutas,
 * así que acá solo se re-exporta (cero duplicación entre verticales).
 */
export { EsqueletoInicioEmpresario as default } from "@/components/portales/PortalSkeletons";
