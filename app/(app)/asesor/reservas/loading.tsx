/**
 * Esqueleto instantáneo de la cola de reservas. El esqueleto real vive UNA sola vez en
 * components/portales/PortalSkeletons: los dos portales comparten árbol de rutas,
 * así que acá solo se re-exporta (cero duplicación entre verticales).
 */
export { EsqueletoReservas as default } from "@/components/portales/PortalSkeletons";
