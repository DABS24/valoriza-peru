/**
 * Esqueleto instantáneo de el catálogo de oportunidades. El esqueleto real vive UNA sola vez en
 * components/portales/PortalSkeletons: los dos portales comparten árbol de rutas,
 * así que acá solo se re-exporta (cero duplicación entre verticales).
 */
export { EsqueletoCatalogo as default } from "@/components/portales/PortalSkeletons";
